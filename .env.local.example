# 이 파일을 .env.local 로 복사해서 사용하세요.
# .env.local 은 git에 커밋되지 않습니다.

# 백엔드 API 주소 (비워두면 Vite proxy 사용 - 개발 시 권장)
# VITE_API_URL=http://localhost:8000

# 백엔드가 다른 서버에 있을 때만 설정
# VITE_API_URL=https://api.example.com
